function playGame(player1, player2) {
    //logical
    if (player1 === "rock" && player2 === "scissors") {
      return "Player 1 is the winner";
    }
  
  
  }
  
  const winner = playGame("rock", "scissors");
  
  console.log(winner);